version: '3.8'

services:
  muleguard:
    build: .
    container_name: muleguard-app
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    volumes:
      - ./:/app
    restart: unless-stopped
